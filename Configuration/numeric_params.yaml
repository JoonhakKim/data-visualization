#this file supports parameter configuration that could be changed by user preference



#weight
group_interval:
  default:
    first_interval: 10T  # e.g., for padding/warm-up
    group_interval: 30T  # main grouping window
    last_interval: 20T   # optional tail window

  APPL:
    first_interval: 10T
    group_interval: 30T
    last_interval: 20T

industry_type:
  default:
    - DUMMY

  BIG_TECH:
    - APPL
    - META
    - ALA
